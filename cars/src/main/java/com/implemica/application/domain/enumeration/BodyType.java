package com.implemica.application.domain.enumeration;

/**
 * The BodyType enumeration.
 */
public enum BodyType {
    SEDAN,
    UNIVERSAL,
    HATCHBACK,
    MINIVAN,
    SUV,
    COUPE,
    CONVERTIBLE,
    PICKUP,
    LIMOUSINE,
}
